apiVersion: v1
kind: ServiceAccount
metadata:
  name: app-sa
  namespace: threat-demo
  annotations:
    # Set this to the output of: terraform -chdir=terraform output -raw irsa_role_arn
    eks.amazonaws.com/role-arn: "<IRSA_ROLE_ARN>"
