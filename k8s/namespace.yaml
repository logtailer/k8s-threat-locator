apiVersion: v1
kind: Namespace
metadata:
  name: threat-demo
  labels:
    name: threat-demo
    # Calico namespace selectors match on these labels
    kubernetes.io/metadata.name: threat-demo
    environment: demo
  annotations:
    purpose: "Threat detection demo namespace — intentionally permissive for Falco testing"
