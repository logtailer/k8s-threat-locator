# Helm values for falcosecurity/falco chart v4.3.0
# Install: helm repo add falcosecurity https://falcosecurity.github.io/charts
#          helm install falco falcosecurity/falco -n falco --create-namespace -f falco/values.yaml

driver:
  # modern_ebpf: built into the Falco binary, no init container, requires kernel 5.8+.
  # EKS AL2023 nodes run kernel 6.x, so this is the right choice for managed node groups.
  # kernel module cannot be loaded on EKS managed nodes (no privileged bootstrap access).
  kind: modern_ebpf

falco:
  json_output: true
  json_include_output_property: true
  json_include_tags_property: true
  log_level: info
  log_stderr: true
  priority: debug
  output_timeout: 2000
  buffered_outputs: false

falcosidekick:
  enabled: true
  replicaCount: 2
  image:
    tag: "2.31.1"
  config:
    aws:
      # useirsa=true makes the chart annotate the falcosidekick ServiceAccount with rolearn
      # so the pod picks up IRSA credentials instead of static keys.
      # rolearn and sns.topicarn are injected at install time by scripts/bootstrap.sh
      # via --set flags so no account-specific values live in this file.
      useirsa: true
      rolearn: ""
      checkidentity: false
      sns:
        topicarn: ""
        minimumpriority: "error"
        rawjson: true
      region: "us-east-1"
    metrics:
      enabled: true
    debug: false
    nooutput: false

tolerations:
  # Allow scheduling on control-plane / master nodes if present
  - key: node-role.kubernetes.io/control-plane
    operator: Exists
    effect: NoSchedule
  - key: node-role.kubernetes.io/master
    operator: Exists
    effect: NoSchedule

# Restrict to Linux — Falco does not support Windows nodes
nodeSelector:
  kubernetes.io/os: linux

affinity:
  nodeAffinity:
    requiredDuringSchedulingIgnoredDuringExecution:
      nodeSelectorTerms:
        - matchExpressions:
            - key: eks.amazonaws.com/nodegroup
              operator: Exists
            # Exclude Fargate — Falco requires kernel module access.
            # Fargate nodes do not expose /proc or allow privileged containers.
            - key: eks.amazonaws.com/compute-type
              operator: NotIn
              values:
                - fargate


resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "200m"
    memory: "256Mi"

# Falco rules are injected at install time from falco/rules/custom-rules.yaml
# via `--set-file customRules.custom-rules\.yaml=...` in scripts/bootstrap.sh.
# That file is the single source of truth — do not re-add a customRules block here.
