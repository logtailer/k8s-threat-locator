## What

<!-- One sentence: what does this change do? -->

## Why

<!-- Why is this change needed? Link to an issue if applicable. -->

## Checklist

- [ ] `make lint` passes
- [ ] `make lambda-test` passes (if touching `lambda/`)
- [ ] `make docker-build` passes (if touching `app/`)
- [ ] README updated if behaviour changed
- [ ] No secrets or kubeconfig paths hardcoded
