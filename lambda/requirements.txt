# boto3 is available in the Lambda runtime; listed here for local dev/testing
boto3

# Kubernetes Python client — used to apply quarantine NetworkPolicy
kubernetes==30.1.0

# boto3 stubs for local type checking (not installed in Lambda)
# boto3-stubs[cloudwatch,s3]

# urllib3 pinned to avoid breaking change in 2.x with older botocore builds
urllib3<2
